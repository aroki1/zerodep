{{- $pages := .RegularPages -}}
{{- if .IsHome -}}
  {{- $pages = where site.RegularPages "Section" "posts" -}}
{{- end -}}
{{- $pages = where $pages "Draft" false -}}
{{- $pages = $pages.ByDate.Reverse -}}
{{- $title := site.Title -}}
{{- if ne .Title site.Title -}}
  {{- $title = printf "%s on %s" .Title site.Title -}}
{{- end -}}
{{- $description := .Params.description | default site.Params.description | default (printf "Recent content on %s" site.Title) -}}
{{- $language := site.Language.Locale | default site.Language.Lang | default "en" -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ $title | html }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ $description | plainify | html }}</description>
    <generator>Hugo</generator>
    <language>{{ $language }}</language>
    {{ with index $pages 0 }}
      <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{ end }}
    {{ with .OutputFormats.Get "RSS" }}
      <atom:link href="{{ .Permalink }}" rel="self" type="application/rss+xml" />
    {{ end }}
    {{ range first 20 $pages }}
      {{- $itemDescription := .Params.description | default .Summary -}}
      <item>
        <title>{{ .Title | html }}</title>
        <link>{{ .Permalink }}</link>
        <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
        <guid>{{ .Permalink }}</guid>
        <description>{{ $itemDescription | plainify | html }}</description>
        {{ range .GetTerms "tags" }}
          <category>{{ .LinkTitle | html }}</category>
        {{ end }}
      </item>
    {{ end }}
  </channel>
</rss>
